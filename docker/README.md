# Docker

This directory contains resources for the production docker container, as well as a container for developers.

Published production images are pushed to GitHub Container Registry on `main` (`:latest`) and on GitHub Releases (tag name). Pull with:

```bash
docker pull ghcr.io/<github-owner>/receipt-wrangler:latest
```
